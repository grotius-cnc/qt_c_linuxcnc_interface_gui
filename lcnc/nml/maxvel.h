#ifndef MAXVEL_H
#define MAXVEL_H

#include <emc.hh>
#include <emc_nml.hh>
#include <iostream>
#include <display/variable.h>

void maxvel(double velocity){
    //const char *cstr = nmlfile.c_str();
    //RCS_CMD_CHANNEL *c = new RCS_CMD_CHANNEL(emcFormat, "emcCommand", "xemc", cstr);

    //finish later on
    //EMC_TRAJ_SET_MAX_VELOCITY ss;

    //m.tag.flags.reset();
    //m.tag.is_valid();
    //m.tag.get_state_tag();

    // void update(CMS * cms);

    //m.velocity=velocity;
    //c->write(&m);
}

#endif // MAXVEL_H
